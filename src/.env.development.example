# ============================================
# DEVELOPMENT ENVIRONMENT
# ============================================
NODE_ENV=development
PORT=3000
HOST=localhost

MONGODB_URI=mongodb://localhost:27017/finance-manager-dev

# Use for development only - generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=dev-secret-key-for-testing-only-123
JWT_ACCESS_EXPIRATION=24h  # Longer for dev
JWT_REFRESH_SECRET=dev-refresh-secret-key-for-testing-only-456
JWT_REFRESH_EXPIRATION=30d

BCRYPT_SALT_ROUNDS=8  # Faster hashing for dev
CORS_ORIGIN=http://localhost:5173

# Mock email for development
SMTP_HOST=localhost
SMTP_PORT=1025  # MailHog port
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

LOG_LEVEL=debug
ENABLE_SWAGGER=true
SHOW_ERROR_DETAILS=true
DEBUG=true